<div class="page-connexion">
  <div class="page-connexion-left">
    <%= image_tag 'page-connexion/page-connexion-inscription-image.jpg' %>
  </div>
  <div class="page-connexion-right">
    <div class="page-connexion-right-inscription">
      <p>Vous avez déjà un compte ?</p>
      <%= link_to new_user_session_path do %>
        <button class="btn-rectangle">Se connecter</button>
      <% end %>
    </div>
    <div class="page-connexion-right-form">
      <h2>Inscription</h2>
      <p>Elèves et professeurs, inscrivez-vous ici.</p>
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email,
                      label: "Adresse email",
                      placeholder: "hello@livesson.com",
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "email" }%>
          <%= f.input :password,
                      label: "Mot de passe",
                      placeholder: "Saississez votre mot de passe",
                      required: true,
                      # hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                      input_html: { autocomplete: "new-password" } %>
          <%= f.input :password_confirmation,
                      label: "Confirmation du mot de passe",
                      placeholder: "Confirmez votre mot de passe",
                      required: true,
                      input_html: { autocomplete: "new-password" } %>
        </div>
        <div class="page-connexion-actu-conditions-generale">
          <input type="checkbox" id="ActuLivesson">
          <label for="ActuLivesson">Je souhaite recevoir l'actualité de Livesson</label>
          <p>En cliquant sur « S'inscrire » j’atteste avoir lu et compris les Conditions Générales d’Utilisation, que j’accepte sans aucune réserve. Je reconnais également avoir été dûment informé et avoir pris connaissance de la Politique de protection des données personnelles.</p>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "S'inscrire", class: "btn-rectangle" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
