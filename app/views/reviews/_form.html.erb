<div class="container">
  <div class="row">
    <div class="col-xs-4">
      <%= simple_form_for @review do |f| %>
        <%= f.input :rating, collection: (1..5).to_a, label: "Évaluer ce cours" %>
        <%= f.input :content, label: false %>
        <%= f.submit "Enregistrer" %>
      <% end %>
    </div>
  </div>
</div>
